import { useState } from 'react'; import { Brand } from './Brand'; import { useAuth } from '../auth';
export function Login(){const{login}=useAuth(),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[loading,setLoading]=useState(false),[error,setError]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();setLoading(true);setError('');try{await login(email,password)}catch{setError('Unable to sign in with those credentials.')}finally{setLoading(false)}};return <main className="flow"><header className="topbar"><Brand/></header><section className="question-card ayush-form"><span className="eyebrow">SWASTHYA SETU</span><h1>Sign in</h1><form onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<p className="consultation-message">{error}</p>}<button className="primary wide" disabled={loading}>{loading?'Signing in…':'Sign in'}</button></form></section></main>}
