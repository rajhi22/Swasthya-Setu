import { api } from './apiClient'; export type AuthUser={id:string;email:string;name:string;role:'patient'|'doctor'|'admin';patientId?:string;doctorId?:string};export const authApi={login:(email:string,password:string)=>api.post<{accessToken:string;user:AuthUser}>('/auth/login',{email,password}),me:()=>api.get<AuthUser>('/auth/me'),logout:()=>api.post<Record<string,never>>('/auth/logout',{})};
