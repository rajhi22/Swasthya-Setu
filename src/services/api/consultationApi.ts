import { api } from './apiClient'; export const consultationApi={get:(caseId:string)=>api.get<any>(`/cases/${caseId}/consultation`),create:(caseId:string,data:unknown)=>api.post<any>(`/cases/${caseId}/consultation`,data),update:(caseId:string,data:unknown)=>api.patch<any>(`/cases/${caseId}/consultation`,data),complete:(caseId:string,data:unknown)=>api.post<any>(`/cases/${caseId}/consultation/complete`,data)};
