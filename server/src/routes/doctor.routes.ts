import { Router } from 'express'; import * as c from '../controllers/doctor.controller.js'; import { requireAuth, requireRole } from '../middleware/auth.middleware.js'; import { requireDoctorProfile } from '../middleware/ownership.middleware.js'; export const doctorRouter=Router();doctorRouter.use(requireAuth,requireRole('doctor','admin'));doctorRouter.get('/:doctorId/cases',requireDoctorProfile,c.list);doctorRouter.get('/:doctorId/cases/:caseId',requireDoctorProfile,c.detail);
