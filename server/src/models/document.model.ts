import { Schema,model } from 'mongoose'; export const Document=model('Document',new Schema({patient:{type:Schema.Types.ObjectId,ref:'PatientProfile',required:true,index:true},case:{type:Schema.Types.ObjectId,ref:'Case',index:true},category:{type:String,enum:['Prescriptions','Lab Reports','Medical Records','Other'],required:true},fileName:{type:String,required:true},fileType:String,storageReference:String,uploadedAt:{type:Date,default:Date.now}},{timestamps:true}));
