import { Schema,model } from 'mongoose'; export const Vital=model('Vital',new Schema({patient:{type:Schema.Types.ObjectId,ref:'PatientProfile',required:true,index:true},case:{type:Schema.Types.ObjectId,ref:'Case'},temperature:Number,bloodPressure:String,heartRate:Number,spo2:Number,bloodGlucose:Number,recordedAt:{type:Date,default:Date.now,index:true},source:{type:String,enum:['patient','doctor'],required:true}},{timestamps:true}).index({patient:1,recordedAt:-1}));
