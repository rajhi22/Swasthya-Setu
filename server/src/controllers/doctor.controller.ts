import type { RequestHandler } from 'express'; import * as s from '../services/doctor.service.js'; const id=(r:any,k:string)=>String(r.params[k]); const send=(f:(r:any)=>Promise<unknown>):RequestHandler=>async(r,res,n)=>{try{res.json({success:true,data:await f(r)})}catch(e){n(e)}}; export const list=send(r=>s.listCases(id(r,'doctorId'),typeof r.query.status==='string'?r.query.status:undefined));export const detail=send(r=>s.caseDetail(id(r,'doctorId'),id(r,'caseId')));
